class Calculadora:
    def __init__(self):
        self.total_operaciones = 0
        print(" Calculadora lista para usar")

    def sumar(self, a, b):
        self.total_operaciones += 1
        return a + b

    def restar(self, a, b):
        self.total_operaciones += 1
        return a - b

    def multiplicar(self, a, b):
        self.total_operaciones += 1
        return a * b

    def dividir(self, a, b):
        self.total_operaciones += 1
        if b == 0:
            return "Error no puedes dividir por 0"
        return a / b
    def potencia(self, base, exponente):
        self.total_operaciones += 1
        resultado = 1
        if exponente >= 0:
            for _ in range(exponente):
                resultado *= base
        else:
            for _ in range(-exponente):
                resultado *= base
            resultado = 1 / resultado
        return resultado

    def raiz_cuadrada(self, numero):
        self.total_operaciones += 1
        if numero < 0:
            return "Error no puede ser un número negativo"
        if numero == 0:
            return 0
        x = numero
        for _ in range(20):
            x = 0.5 * (x + numero / x) 
        return x

    def factorial(self, n):
        self.total_operaciones += 1
        if n < 0:
            return "Error factorial  negativos"
        elif n == 0 or n == 1:
            return 1
        else:
            resultado = 1
            for i in range(2, n + 1):
                resultado *= i
            return resultado
